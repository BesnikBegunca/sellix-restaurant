/// Application version labels (keep in sync with [pubspec.yaml] `version:`).
abstract final class AppVersionInfo {
  static const String version = '1.0.0';
  static const String buildNumber = '1';
}
